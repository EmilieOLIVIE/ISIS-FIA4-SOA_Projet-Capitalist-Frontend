() => {
    return (<>
        {/* <input
                                type="image"
                                className="productImage"
                                name={product.name}
                                src={"http://localhost:4000/" + product.logo}
                                onClick={(event) => console.log(event.currentTarget.name)}
                            /> */}
        {/* <button className="productImage">
                                <img src={"http://localhost:4000/" + product.logo} className="productImage" />
                            </button> */}
        {/* <button className="productImage" style={{ background: `url(http://localhost:4000/${product.logo})` }} /> */}
    </>)
}

